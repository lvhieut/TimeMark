<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:strokeAttrs="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:background="#F5F5DC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".screen.SplashActivity">

    <com.example.timemarkbase.custom_view.StrokedTextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:fontFamily="@font/rowdies"
        android:includeFontPadding="false"
        android:text="@string/app_title"
        android:textColor="@color/black"
        android:textSize="@dimen/dimen_30sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        strokeAttrs:textHasShadow="true"
        strokeAttrs:textStrokeColor="#EBD50F"
        strokeAttrs:textStrokeWidth="3" />

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        app:indicatorColor="#f4b309"
        app:indicatorSize="64dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:trackColor="@color/white"
        android:layout_marginBottom="40dp"
        app:trackCornerRadius="16dp"
        app:trackThickness="8dp" />

</androidx.constraintlayout.widget.ConstraintLayout>